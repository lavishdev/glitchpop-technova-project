class AppConstants {
  const AppConstants._();

  static const String appName = 'CrowdShield';
  static const Duration apiTimeout = Duration(seconds: 30);
  static const Duration defaultAnimationDuration = Duration(milliseconds: 300);
}
